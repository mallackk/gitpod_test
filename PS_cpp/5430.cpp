// 만약 vector로 구현한다면
// D, R 구현 가능.
// queue로 구현해도 가능?

#include <iostream>
#include <vector>
#include <deque>
#include <algorithm>
#include <string>

using namespace std;

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int test;
    int n;
    cin >> test;
    while(test--) {
       deque<int> dq;
       string order;
       string str; 
       cin >> order; // RDD
       cin >> n; // 횟수
       cin >> str; // 문자열로 받아서 원소 개수 세야됨
       string s = "";
      
        
    }
    
}
